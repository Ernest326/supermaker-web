import { leap } from '@onehop/client';
import { API } from '@onehop/js';
export declare function useChannelMessage<T = any>(channel: API.Channels.Channel['id'], event: string, listener: (data: T) => unknown): void;
export declare function useReadChannelState<T extends API.Channels.State = API.Channels.State>(channel: API.Channels.Channel['id']): leap.ChannelStateData<T>;
